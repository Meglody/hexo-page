{
  "name": "hexo-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "hexo clean",
    "server": "hexo server",
    "prebuild": "./prebuild.bash",
    "build": "npm run clean && hexo generate",
    "deploy": "npm run clean && hexo deploy",
    "predev": "./prebuild.bash",
    "dev": "npm run clean && cross-env NODE_ENV=development && npm run server"
  },
  "hexo": {
    "version": "5.4.0"
  },
  "dependencies": {
    "@types/three": "^0.135.0",
    "hexo": "^5.0.0",
    "hexo-deployer-git": "^3.0.0",
    "hexo-generator-archive": "^1.0.0",
    "hexo-generator-category": "^1.0.0",
    "hexo-generator-index": "^2.0.0",
    "hexo-generator-tag": "^1.0.0",
    "hexo-render-pug": "^2.1.4",
    "hexo-renderer-ejs": "^2.0.0",
    "hexo-renderer-marked": "^4.0.0",
    "hexo-renderer-stylus": "^2.0.1",
    "hexo-server": "^2.0.0",
    "hexo-theme-landscape": "^0.0.3",
    "three": "^0.142.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
